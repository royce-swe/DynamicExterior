## Packages
framer-motion | Page transitions and scroll-triggered animations
lucide-react | High quality icons for services and UI elements

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  sans: ["var(--font-sans)"],
}
Smooth scrolling is enabled natively via CSS.
The application uses a single-page landing structure with section anchors.
